# Control-plane supplements v3

`j_bound_heterogeneous_*` is a CPU candidate-heterogeneity simulation. `budget_freshness_*` is a repeated finite-link simulation with confidence intervals. Neither file is live vLLM/network evidence.
