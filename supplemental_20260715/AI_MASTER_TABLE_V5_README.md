# AI Master Table V5

V5 appends SLO-aware paired replay, single-host owner/transport microbenchmarks, and (when complete) the difficult live K sweep to V4. Filter by `status`, `source_dataset`, and `evidence_type`; do not compare metrics across evidence types as if they were one serving benchmark.
