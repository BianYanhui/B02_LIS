# AI Master Table v3

This flat table contains all V2 evidence plus V3 reviewer-driven supplements. Use `source_dataset`, `evidence_type`, and `metric_scope` before comparing rows. The source AgentTrace raw JSONL is intentionally excluded.
