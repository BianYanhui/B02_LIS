# Paired T4 latency replay v3

Each repetition shares one logical trace across interfaces and randomizes policy order. Use paired median/IQR/CI results, not an unpaired mean p95. Reuse is dispatcher-observed.
