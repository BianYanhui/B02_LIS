# Single-host owner and metadata transport microbenchmark (V5)

Uses loopback TCP and binary frames on one host. It measures serialization/IPC and a reference owner validation path only; it is not an inter-node network benchmark or a vLLM-native KV pin implementation.
